\section{Symmetric factorization}
On $\beta_1=\beta_2=b$, the numerator of $D_1$ is
\[
-\gamma(a-c)(\lambda-3)^2
\left[b(\lambda^2-3\lambda+4)+\lambda^2-\lambda-2\right].
\]
On the regular domain, the nonzero prefactors can be removed without changing the equality set, leaving Eq. \eqref{eq:symcross}.
