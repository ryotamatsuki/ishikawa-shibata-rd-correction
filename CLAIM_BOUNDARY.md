# Claim Boundary

## Claims we can make

1. The published output-stage equilibrium and the published closed-form noncooperative/cooperative R&D solutions used in the audit remain valid.
2. For `D_i(beta_1,beta_2,lambda,gamma)=x_i^n-x_i^c`, the individual competition/cooperation threshold is determined by `D_i=0` and generically depends on `gamma`.
3. On the symmetric slice `beta_1=beta_2=b`, the crossing is `b(lambda)=(2+lambda-lambda^2)/(lambda^2-3lambda+4)` and is exactly gamma-independent.
4. For `D_A=(x_1^n+x_2^n)-(x_1^c+x_2^c)`, the aggregate equality set `D_A=0` is generically a gamma- and asymmetry-dependent curve, not generally reducible to `beta_1+beta_2=theta(lambda)`.
5. At `lambda=0`, on the published asserted line `beta_1+beta_2=1`, the aggregate equality numerator is proportional to `(beta_1-beta_2)^2[18 gamma-9+(beta_1-beta_2)^2]`; for maintained `gamma>=1`, asymmetric points on that line are not equality points.
6. At `a=100,c=50,gamma=10,lambda=0,beta_1=0.98,beta_2=0`, the published fixed-cutoff classification is strictly reversed by the corrected aggregate comparison; continuity implies a positive-measure disagreement region.

## Claims requiring qualification

- The paper's third headline interpretation requires qualification because the fixed average-spillover cutoff logic is false on part of the admissible asymmetric domain.
- The broad directional claim that stronger market competitiveness tends to enlarge the competitive-R&D region was not globally overturned; Stage 3 only found targeted corrected sweeps consistent with that direction.
- Table 1's displayed `gamma=50` benchmark values reproduce, but their interpretation as gamma-invariant/general asymmetric thresholds does not.
- Fig. 1(d) can be valid on its special slice while failing to establish generic gamma-invariance.
- Eq. (47)/Fig. 4 branch locations are locally affected near the corrected aggregate boundary; Observation 6 survives the published calibration recomputation.

## Claims we cannot make

1. The entire paper is wrong.
2. The published closed-form equilibrium formulas are wrong.
3. Proposition 1 is overturned.
4. Proposition 2 is overturned.
5. Observation 6 is overturned.
6. The broad effect of higher market competitiveness is reversed globally.
7. Social welfare ranking is reversed.
8. Welfare analysis is invalid.
9. The error affects empirical estimates.
10. The correction has already been proven to affect downstream literature.
11. No prior disclosure exists.
12. This is definitively the first correction.
13. The corrected boundary is globally monotone in every parameter.
14. A global nesting theorem in `lambda` has been proven.

## Novelty wording control

Classification: `N-A — NO_PRIOR_DISCLOSURE_FOUND_UNDER_PUBLICATION_GRADE_SEARCH`.

Permissible statement only:

> No prior disclosure covering the same correction was found under the documented search protocol as of 2026-09-01.

Do not write “This correction is novel,” “No correction exists,” “This is the first correction,” or “The literature has missed this error.”

## Consequence controls

Controlled labels:

- `PARAMETER_REGION_CHANGED`
- `INTERPRETATION_CHANGED`

Severity: `LEVEL 3 — qualitative classification changes for admissible parameters`.

Publication-value classification: `MEDIUM-HIGH`.

Do not assign `EQUILIBRIUM_CHANGED`, `WELFARE_RANKING_CHANGED`, or `EMPIRICAL_ESTIMATE_CHANGED`.

## Terminology controls

The paper's relevant numerical-section term “social preference” is operationalized through aggregate R&D comparison in this audit. Do not relabel that object as “social welfare.” Do not translate an `aggregate R&D ranking` into a `welfare ranking`.
